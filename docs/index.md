# Intermediate Feature Engineering Docs
- `min_max_scale(data)`: Scales data to [0, 1].
- `standard_scale(data)`: Standardizes data (mean=0, std=1).
- `one_hot_encode(categories)`: One-hot encodes categorical data.